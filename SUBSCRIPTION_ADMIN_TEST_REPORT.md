# 订阅与管理接入验收

日期：2026-09-19。前端 3310，现有本地后端 3200。

## 来源和范围

- 订阅 UI 使用 `D:\下载\订阅.zip` 的 `components/membership` 和所需 UI 原语。
- 删除上一版普通套餐卡片实现及整个假管理页面文件。
- 套餐页保留黑底、横向栏、周期切换、积分档位、权益分组、购买确认。
- 保留此前取消顶部活动横幅的选择。没有复制参考包的虚拟售价或业务承诺。
- 新后台商品和订单页接入真实接口，其余管理能力通过 `/management` 进入现有完整后台。

## 实际发现并修复

1. 普通 API rewrite 导致浏览器 POST/PATCH 被后端 CSRF 校验拦截。改为先验证外部 Origin/Referer，再转发至固定后端；保留 Cookie 和流式正文。
2. checkout 返回 `code/data/checkout`，原实现读取顶层字段报错。已按接口结构解包。
3. 参考 UI 把带小数的价格四舍五入、将年费标为月价。现保留小数且整期计价。
4. 不同周期/档位使用对应商品 ID；没有购买价格的周期不展示该商品。
5. 账户订单列表原为演示数据，已改为真实用户订单接口。
6. 弹窗通过 portal 渲染后丢失深色 CSS 变量，已补齐弹窗自身变量。

## 已验证

- 浏览器实际登录本地管理员，并从登录页回到 `/admin/products`。
- UI 新增测试商品，价格 12.34 元，数据库 API 返回 1234 分，保存名称及两项权益。
- 订阅页读取测试商品，显示 12.34 元和保存的权益。
- UI 下单，真实待支付订单金额为 1234 分，显示人工支付信息，可查询订单状态。
- UI 编辑测试商品为 15.67 元，API 返回 1567 分。
- UI 下架测试商品，公共商品 API 不再返回此商品。
- 管理订单页和账户订单页读取真实测试订单。
- 两笔待支付测试订单已取消，测试商品已下架。没有付款或发放测试积分。
- 未登录管理接口返回 401；外站 Origin 的写入返回 403。
- `/management?section=payments` 可进入现有后端支付管理页并沿用登录会话。
- 桌面 1440px、移动端 390px 的订阅及后台页面无全页横向溢出，无页面 JavaScript 异常。
- 类型检查通过；生产构建包含 TypeScript 检查并通过。
- 5 个计价映射回归测试通过：价格精度、年费、显式分组、下架过滤、重复周期商品。

## 未验证和剩余范围

- 本地支付提供方为 manual，未向外部支付机构付款，未验收真实支付回调、退款或财务结算。
- 二维码使用 qrcode 实际生成；本次没有可用的真实扫码支付提供方进行付款验收。
- 新前端原有 Agent、图像/视频生成等模块不在本次管理与订阅验收范围内，不能据此宣称全站业务已对接完成。
- 参考包的商品数量、优惠及生成量示例不代表现有后台配置。前台按实际启用商品展示，完整五档可在管理端配置。
