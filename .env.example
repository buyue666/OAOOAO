# 后端服务地址。必须是前端服务器可访问的地址，不是浏览器地址。
OAOAO_BACKEND_URL=http://127.0.0.1:3200

# 站点对外源。生产环境必须设置为与浏览器访问完全一致的地址（含协议）。
# 未设置时会回退为按请求头推断，仅适合单节点本地开发。
# 写入请求（POST/PATCH/DELETE）会用它校验 Origin/Referer；不一致会返回 403。
# OAOAO_PUBLIC_URL=https://studio.example.com

# 生产部署的环境变量、HTTPS、Cookie、反向代理与支付配置见 DEPLOYMENT.md。
